[pytest]
testpaths = satellite_processor/core/tests
python_files = test_*.py
addopts = -v --tb=short
qt_api = pyqt6
log_cli = true
log_cli_level = INFO

# Optionally, add markers for pytest-qt to handle GUI tests properly
markers =
    qt: mark tests as Qt tests
